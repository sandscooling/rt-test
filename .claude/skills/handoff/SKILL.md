---
name: handoff
description: Hand this session's work to a fresh successor session instead of letting the context compact. Use when the user asks for a handoff or a successor, or when the context is past about 60% of its window (75% for the orchestrator).
---

Follow `_agent-docs/handoff.md` from the repository root, starting with "Steps for the old session". That file is the only copy of the procedure; do not work from memory of it.
